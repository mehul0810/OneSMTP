# Aculect Mail 0.3.0 Provider MVP Testing Readiness

Use this checklist to prove the 0.3.0 provider MVP on a staging or local site before any release-facing packaging work. This is a readiness artifact, not a feature claim.

## Scope

- Provider setup, activation, priority, weight, and safe credential entry.
- Provider-specific test send flows and their safe success or failure responses.
- Delivery logging, provider selection context, attempt lineage, and redacted failure details.
- Operational readiness signals for DNS, sender identity, and retry/log visibility.

## Quality Gate Matrix

| Gate | Status | Current evidence |
| --- | --- | --- |
| Security/privacy | Pass | Redaction boundaries and synthetic fixtures are already covered in `tests/Unit/Admin/LogAdminTest.php`, `tests/Unit/Api/RestControllerTest.php`, and the fixture-only browser smoke path. |
| Performance | Pass with follow-up | The initial admin entry bundle is 6.17 KiB and DataViews is screen-level lazy loaded. The 2026-08-06 smoke benchmark passed 1,000 dispatch decisions and synthetic 20,000-message/100,000-attempt log paths; the 1.68 MiB DataViews vendor chunk remains a post-release optimization target. |
| Modularity/architecture | Pass | Current evidence stays inside small provider/admin/logging boundaries and the provider MVP lane is split across focused unit, integration, and E2E tests. |
| Maintainability | Pass | The release-readiness note is isolated from feature docs, and the existing tests keep the 0.3.0 evidence traceable by boundary. |
| Test coverage | Pass | Unit, integration, and E2E evidence already exists for provider types, REST behavior, log handling, compatibility, and the controlled admin smoke fixture. |
| Documentation/readme/changelog/release notes | Pass | `readme.txt`, `CHANGELOG.md`, `package.json`, and `docs/release/0.3.0-release-notes.md` describe the current release candidate without claiming guaranteed delivery. |
| Commented-out/debug/TODO/dead-code hygiene | Pass | No release-facing debug scaffolding is introduced here; remaining TODOs live in test/support scaffolding rather than production paths. |
| Compatibility | Pass | `tests/Integration/Dispatch/CompatibilityMatrixTest.php` and `docs/developer/compatibility-matrix.md` already cover the synthetic source families used for current readiness evidence. |
| Package/version/WordPress.org metadata | Pass | `onesmtp.php`, `package.json`, and `readme.txt` advertise 0.3.0. Final ZIP inspection and publication approval remain separate gates. |
| UI/browser/Playwright screenshot proof | Blocked | Browser smoke coverage exists, but there is no committed 0.3.0 screenshot artifact yet; only the `docs/release/0.1.0-screenshots-checklist.md` guidance is present. |

### Current 0.3.0 Evidence Lane

- This branch is based on `origin/release/0.3.0` at merge commit `a42a05e`, which is the merge of PR `#134` from issue `#133-provider-mvp-testing-readiness`.
- The lane established by PR `#134` is the provider MVP testing readiness checklist in this file; this matrix classifies the current evidence without claiming Pro-only scope or future packaging completion.
- Current proof is strongest around provider CRUD, safe test-send handling, logging, compatibility, and the controlled admin browser fixture.

## Automated And Local Proof

- `tests/Unit/Providers/ProviderTypesTest.php` covers supported provider metadata and capability flags.
- `tests/Unit/Api/RestControllerTest.php` covers provider CRUD validation, safe provider reads, and provider test-send responses.
- `tests/Unit/Admin/ProviderAdminTest.php` covers provider admin state and form behavior.
- `tests/Unit/Admin/LogAdminTest.php` covers log filtering, redaction, resend eligibility, and safe detail views.
- `tests/Integration/Dispatch/CompatibilityMatrixTest.php` covers representative synthetic WordPress email sources against the send pipeline.
- `tests/Integration/Dispatch/LoggingIntegrationTest.php` covers attempt logging, provider context, and lineage output.
- `tests/E2E/Admin/DeliverabilitySmokeTest.php` covers the controlled admin smoke fixture without real credentials or external delivery.
- Local fake proof should use synthetic providers, synthetic recipients, and test doubles only.

## Manual Real-Provider Proof

- Configure Aculect Mail on a staging or local WordPress install first.
- Use a disposable inbox or disposable domain for any real-provider check.
- Use a real provider credential only in the staging or local environment under test.
- Send a provider test email from the provider workflow and confirm delivery in the disposable inbox.
- Confirm the provider test response is safe and does not expose secrets, tokens, or provider message IDs.
- Confirm Email Logs show the expected provider, status, attempt number, and redacted error context.
- If the provider requires sender authentication, complete SPF, DKIM, and DMARC prerequisites before retrying the test send.
- If failover is part of the test plan, repeat with a secondary provider and confirm the fallback path is visible in logs.

## Owner Prerequisites

- Disposable inbox or disposable domain.
- Provider credential for the chosen adapter.
- DNS access when sender authentication is required.
- Staging or local WordPress site preferred over production.
- Admin access to Aculect Mail settings, provider setup, and email logs.

## Release-Facing Proof Gaps

- A fresh 0.3.0 performance artifact and visual screenshot set are still required.
- Real-provider delivery and controlled failover require owner-supplied staging credentials and a disposable inbox.
- The final release ZIP must be inspected after all automated gates pass.
- Publishing, tagging, and releasing remain explicit owner-approved actions.

## Future Release-Ready Recommendation

- Keep `onesmtp.php`, `package.json`, `readme.txt`, and `CHANGELOG.md` aligned as the release candidate changes.
- Add committed screenshot proof if the release process requires WordPress.org assets, then keep the 0.1.0 checklist as the capture procedure.
- If a fresh release gate is needed, run the lightweight validation listed below and attach the exact outputs to the PR.

## Later Validation To Run

```bash
git diff --check
vendor/bin/phpunit -c phpunit.xml.dist tests/Unit/Providers/ProviderTypesTest.php tests/Unit/Api/RestControllerTest.php tests/Unit/Admin/ProviderAdminTest.php tests/Unit/Admin/LogAdminTest.php tests/Integration/Dispatch/LoggingIntegrationTest.php tests/Integration/Dispatch/CompatibilityMatrixTest.php
npm run test:e2e
```

## Pass Criteria

- Automated and local fake proof passes without real provider credentials.
- Manual provider proof is completed on a staging or local site using disposable test data.
- Release-facing metadata gaps are explicitly understood and either accepted for later work or addressed in a separate release-prep change.
